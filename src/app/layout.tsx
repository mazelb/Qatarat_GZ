import '../styles/globals.css';

/**
 * Root layout - required but mostly empty as the real layout is in [lang]
 */
export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}